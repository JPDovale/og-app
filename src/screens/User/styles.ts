import { cva } from 'class-variance-authority'

export const userPageStyles = cva(['bg-gray900'])

export const userInfosContainer = cva(['p-4'])

export const avatarContainerStyles = cva(['items-center', 'flex-col', 'gap-4'])

export const infosStyles = cva(['flex-col', 'w-full', 'mt-6'])
