import { cva } from 'class-variance-authority'

export const notificationsPageStyles = cva(['bg-gray900'])

export const notificationsContainerStyles = cva(['p-4'])

export const notificationCardStyles = cva([
  'bg-gray700',
  'mb-6',
  'p-4',
  'rounded-md',
  'shadow-black',
  'flex-1',
])
