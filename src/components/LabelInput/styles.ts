import { cva } from 'class-variance-authority'

export const labelContainerStyles = cva(['w-full'])

export const infosLabelStyles = cva([
  'flex-row',
  'w-full',
  'items-center',
  'justify-between',
  'mb-1',
])
